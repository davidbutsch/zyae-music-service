// import { AccountModel } from "@/modules/account";

// type UserDTO = any;

// export const createProfile = async (user: UserDTO) => {
//   const newProfileDocument = new AccountModel({
//     profile: {
//       email: user.profile.email,
//       name: user.profile.name,
//       shortName: user.profile,
//     },
//   });

//   newProfileDocument;
// };

// // step 1: figure out how controller -> service -> repo model is gonna work
// // step 2: figure out how to create service/repo interfaces and make service/repo dependencies injectable
// // step 3: die
